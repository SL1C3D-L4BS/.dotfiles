#!/usr/bin/env bash
# ══════════════════════════════════════════════════════════════
# sl1c3d-cli — launch SL1C3dCLI (always uses latest dist/ build)
# ══════════════════════════════════════════════════════════════
APP_DIR="$HOME/dev/projects/sl1c3d-cli"
cd "$APP_DIR" || exit 1
exec npx electron . "$@"
