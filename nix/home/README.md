# nix/home — Package sets (Phase 8)

Reserved for Nix package definitions (core CLI, languages, dev tools). Configs remain in chezmoi. Phase 8 implements `nix profile install` or flake outputs from here.
