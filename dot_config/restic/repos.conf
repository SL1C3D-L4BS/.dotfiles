# ─────────────────────────────────────────────────────────────────────────────
# Restic backup repos — SL1C3D-L4BS
# Set password once: secret-tool store --label="restic" service restic repo local
# Export before use: export RESTIC_PASSWORD=$(secret-tool lookup service restic repo local)
# ─────────────────────────────────────────────────────────────────────────────

# Local backup (external drive / NAS)
RESTIC_REPOSITORY="/mnt/backup/sl1c3d-l4bs"

# B2 cloud backup (alternative — uncomment and set B2 credentials)
# RESTIC_REPOSITORY="b2:sl1c3d-l4bs-backup:/"
# B2_ACCOUNT_ID=""
# B2_ACCOUNT_KEY=""

# Initialize first time: restic init
