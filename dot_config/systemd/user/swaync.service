[Unit]
Description=swaync notification daemon (SL1C3D-L4BS)
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/swaync
Restart=on-failure
RestartSec=2
# Doctor severity: degraded

[Install]
WantedBy=graphical-session.target
