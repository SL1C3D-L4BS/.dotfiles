# theme-engine — Theme source and generator

**Current live artifacts:** `dot_config/SL1C3D-L4BS/theme/` (generate.py, executable_generate.sh) and repo `theme.tokens.toml`. Entrypoint: `sl1c3d theme apply`.

Until migrated here, theme pipeline stays under dot_config. Migration ledger: `docs/architecture/migration-ledger.md`.
