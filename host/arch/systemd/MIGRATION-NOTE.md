# systemd/ — System (machine) units

No system-level units in use; user units are in `dot_config/systemd/user/`. This directory is reserved for future host systemd drops (e.g. system-wide timers or services). No migration pending.
